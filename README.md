# Langchain-and-DBvector-Store
LangChain is an open-source framework designed to build applications powered by Large Language Models (LLMs) like GPT-4, by connecting them to external data sources, APIs, and memory. 

DataIngestion -> Data split -> Text to Vector -> VectorDB  -> similarity search with external query

